import React from 'react';
import ReactDOM from 'react-dom/client';
import './backroads.css';
import AppBackroads from './AppBackroads'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AppBackroads />
  </React.StrictMode>
);
